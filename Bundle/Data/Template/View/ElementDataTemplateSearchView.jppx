class ElementDataTemplateSearchView extends ContentView {

	_class = 'element';
	_elementType = 'Element';

	#default get for _class;
	#default get for _elementType;

	initialize : function () {
		_super();
		this._template.getNext();
	}

	initialState : function () {
		var store = serviceLocator.get('ServiceManager').get('store_manager_main_service');
		return ({
			loader : true,
			students : store.getAll(this.getElementType())._entity
		});
	}

	update : function () {
		var store = serviceLocator.get('ServiceManager').get('store_manager_main_service');

		this.setState({ students : store.getAll(this.getElementType())._entity });
	}

	display : function () {
		var self = this;
		var loader = null;

		if (this.state.loader) {
			loader = this.getTemplate('navigation_loader_main_template');
		}

		return (<div className={this.getClass() +'s'} key={this.props.key}>
			{Object.keys(this.state.students).map(function (i) {
				if (self.state.students[i].isMe())
					return null;
				return (<div className={this.getClass()} key={i}>
					{self.state.students[i].displayTimeline()}
				</div>);
			})}
			<div className="loader_students">
				{loader}
			</div>
		</div>);
	}

}